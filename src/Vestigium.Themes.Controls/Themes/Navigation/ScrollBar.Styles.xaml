<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =======================================================================
         COMMON UI SCROLLBAR STYLES
         =======================================================================
         Usage: 
         <ScrollBar Style="{StaticResource ScrollBar.Standard}" Orientation="Vertical" />
         <ScrollBar Style="{StaticResource ScrollBar.Minimal}" Orientation="Vertical" />

         Available Styles:
         - ScrollBar.Standard : Modern flat scrollbar with line buttons (arrows)
         - ScrollBar.Minimal  : Compact thumb-only scrollbar (no arrows)
         ======================================================================= -->

    <!-- ========================================== -->
    <!-- 1. THUMB                                   -->
    <!-- ========================================== -->
    <Style x:Key="ScrollBar.Thumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{DynamicResource Vestigium.Brushes.Scroll.Thumb}"
                            CornerRadius="4"
                            Margin="2" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Vestigium.Brushes.Scroll.ThumbHover}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Vestigium.Brushes.Scroll.ThumbDrag}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Vestigium.Brushes.Stroke.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================== -->
    <!-- 2. PAGE BUTTON (track click area)          -->
    <!-- ========================================== -->
    <Style x:Key="ScrollBar.PageButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================== -->
    <!-- 3. LINE BUTTON (arrow buttons)             -->
    <!-- ========================================== -->
    <Style x:Key="ScrollBar.LineButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource Vestigium.Brushes.Text.Secondary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border x:Name="ButtonBorder"
                            Background="Transparent"
                            Margin="1"
                            CornerRadius="2">
                        <Path Data="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}"
                              Fill="{TemplateBinding Foreground}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Vestigium.Brushes.Stroke.Subtle}" />
                            <Setter Property="Foreground" Value="{DynamicResource Vestigium.Brushes.Accent.Primary}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Vestigium.Brushes.Stroke.Strong}" />
                            <Setter Property="Foreground" Value="{DynamicResource Vestigium.Brushes.Accent.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Vestigium.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================== -->
    <!-- 4. STANDARD TEMPLATES (with arrows)        -->
    <!-- ========================================== -->

    <!-- Vertical + arrows -->
    <ControlTemplate x:Key="ScrollBar.Template.Vertical" TargetType="ScrollBar">
        <Grid Background="{DynamicResource Vestigium.Brushes.Surface.Header}" Width="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.RowSpan="3" BorderBrush="{DynamicResource Vestigium.Brushes.Stroke.Subtle}" BorderThickness="1,0,0,0" />

            <RepeatButton Grid.Row="0"
                          Height="14"
                          Style="{StaticResource ScrollBar.LineButton}"
                          Command="ScrollBar.LineUpCommand"
                          Content="M 0,4 L 4,0 L 8,4 Z" />

            <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageUpCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBar.Thumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageDownCommand" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton Grid.Row="2"
                          Height="14"
                          Style="{StaticResource ScrollBar.LineButton}"
                          Command="ScrollBar.LineDownCommand"
                          Content="M 0,0 L 4,4 L 8,0 Z" />
        </Grid>
    </ControlTemplate>

    <!-- Horizontal + arrows -->
    <ControlTemplate x:Key="ScrollBar.Template.Horizontal" TargetType="ScrollBar">
        <Grid Background="{DynamicResource Vestigium.Brushes.Surface.Header}" Height="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.ColumnSpan="3" BorderBrush="{DynamicResource Vestigium.Brushes.Stroke.Subtle}" BorderThickness="0,1,0,0" />

            <RepeatButton Grid.Column="0"
                          Width="14"
                          Style="{StaticResource ScrollBar.LineButton}"
                          Command="ScrollBar.LineLeftCommand"
                          Content="M 4,0 L 0,4 L 4,8 Z" />

            <Track x:Name="PART_Track" Grid.Column="1" IsDirectionReversed="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageLeftCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBar.Thumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageRightCommand" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton Grid.Column="2"
                          Width="14"
                          Style="{StaticResource ScrollBar.LineButton}"
                          Command="ScrollBar.LineRightCommand"
                          Content="M 0,0 L 4,4 L 0,8 Z" />
        </Grid>
    </ControlTemplate>

    <!-- ========================================== -->
    <!-- 5. MINIMAL TEMPLATES (thumb only)          -->
    <!-- ========================================== -->

    <!-- Vertical – no arrows -->
    <ControlTemplate x:Key="ScrollBar.Template.Minimal.Vertical" TargetType="ScrollBar">
        <Grid Background="Transparent" Width="10">
            <Border BorderBrush="{DynamicResource Vestigium.Brushes.Stroke.Subtle}" BorderThickness="1,0,0,0" />

            <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageUpCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBar.Thumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageDownCommand" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <!-- Horizontal – no arrows -->
    <ControlTemplate x:Key="ScrollBar.Template.Minimal.Horizontal" TargetType="ScrollBar">
        <Grid Background="Transparent" Height="10">
            <Border BorderBrush="{DynamicResource Vestigium.Brushes.Stroke.Subtle}" BorderThickness="0,1,0,0" />

            <Track x:Name="PART_Track" IsDirectionReversed="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageLeftCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBar.Thumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBar.PageButton}" Command="ScrollBar.PageRightCommand" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <!-- ========================================== -->
    <!-- 6. MAIN STYLES                             -->
    <!-- ========================================== -->

    <!-- Standard – with line buttons -->
    <Style x:Key="ScrollBar.Standard" TargetType="ScrollBar">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource ScrollBar.Template.Vertical}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource ScrollBar.Template.Horizontal}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Minimal – thumb only, narrower -->
    <Style x:Key="ScrollBar.Minimal" TargetType="ScrollBar">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource ScrollBar.Template.Minimal.Vertical}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource ScrollBar.Template.Minimal.Horizontal}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>